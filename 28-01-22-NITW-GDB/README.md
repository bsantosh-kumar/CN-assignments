Format for executing client program is. 

*./client exit* **For telling the server to exit**

*./client program `<program name>` **Client program is going to send contents of `<program name>` to the server**
